services:
  frontend:
    build: ./FrontEnd
    env_file:
      .env
    ports:
      - 80:80
    depends_on: 
      - backend
  backend:
    build: ./backend
    ports:
      - 81:5000
    env_file:
      - .env
    
 
     
    